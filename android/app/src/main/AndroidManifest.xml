<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="com.goldenoremar.app"
                    android:host="auth"
                    android:pathPrefix="/callback" />
            </intent-filter>

            <!-- Android App Links: public product, producer, category and event
                 pages open in the app instead of the browser.
                 autoVerify asks Android to confirm ownership through
                 https://<host>/.well-known/assetlinks.json. Until that file
                 carries this app's signing-certificate fingerprint, Android
                 treats these as ordinary links and opens the browser, so the
                 filter is safe to ship before verification is set up.
                 Data elements combine as scheme x host x path, so every listed
                 host accepts every listed path prefix. Only public, shareable
                 paths are claimed; account, cart and admin are left to the
                 browser on purpose. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="goldenoremar.com" />
                <data android:scheme="https" android:host="www.goldenoremar.com" />
                <data android:scheme="https" android:host="golden-oremar.vercel.app" />
                <data android:pathPrefix="/urun/" />
                <data android:pathPrefix="/uretici/" />
                <data android:pathPrefix="/kategori/" />
                <data android:pathPrefix="/etkinlikler/" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Android 13+ runtime permission used by the Capacitor push-notifications client. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
