import React,{useEffect,useState}from'react';
import{Loader2}from'lucide-react';
import AdminOfficialStoreProducts from'./AdminOfficialStoreProducts';
import{loadOfficialStoreProductManager}from'./officialStoreProductApi';
import StoreBrandingEditor from'../features/store-branding/StoreBrandingEditor';

export default function AdminOfficialStoreWorkspace(){const[producerId,setProducerId]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true);async function load(){try{setLoading(true);setError('');const result=await loadOfficialStoreProductManager();const id=String(result.officialStore?.id||'').trim();if(!/^[0-9a-f-]{36}$/i.test(id))throw new Error('Golden Oremar resmi mağaza kimliği doğrulanamadı.');setProducerId(id);}catch(next){setProducerId('');setError(String((next as {message?:unknown})?.message||'Golden Oremar resmi mağaza kimliği yüklenemedi.'));}finally{setLoading(false);}}useEffect(()=>{void load();},[]);return<div className="space-y-6">{loading?<div role="status" className="flex min-h-32 items-center justify-center gap-2 rounded-2xl border"><Loader2 aria-hidden="true" className="h-5 w-5 animate-spin"/>Mağaza kimliği yükleniyor...</div>:producerId?<StoreBrandingEditor producerId={producerId} title="Golden Oremar mağaza kimliği" description="Resmi mağazanın profil logosu ve kapak görseli yalnız product.publish yetkili Super Admin tarafından değiştirilebilir. Yeni görseller cihaz ve sunucuda ölçü, binary ve Storage bütünlüğü doğrulamasından geçer."/>:<div role="alert" className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800">{error}</div>}<AdminOfficialStoreProducts/></div>;}
