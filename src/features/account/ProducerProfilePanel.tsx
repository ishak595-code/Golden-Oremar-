import React,{useEffect,useState}from'react';
import{CheckCircle2,MapPin}from'lucide-react';
import{Panel,ErrorState,LoadingState}from'./ui';
import{supabase}from'../../lib/supabase';
import StoreBrandingEditor from'../store-branding/StoreBrandingEditor';

const activeLocationStatuses=new Set(['pending','under_review']);
const UUID_RE=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
async function rpc(name:string,args:any={}){const{data,error}=await supabase.rpc(name,args);if(error)throw error;return data;}
function normalizedId(value:unknown){const id=String(value||'').trim();return UUID_RE.test(id)?id:'';}

export default function ProducerProfilePanel({onChanged}:{onChanged?:()=>void|Promise<void>}){
 const[profile,setProfile]=useState<any>(null),[request,setRequest]=useState<any>(null),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState('');
 const[form,setForm]=useState({displayName:'',description:'',story:''});
 const[location,setLocation]=useState({countryCode:'',province:'',district:'',village:'',villageIsCustom:false,reason:''});
 async function load(silent=false){try{if(!silent)setLoading(true);setError('');const[p,r]=await Promise.all([rpc('get_my_producer_profile_v1'),rpc('get_my_producer_location_change_v1')]);if(!p||typeof p!=='object'||Array.isArray(p)){setProfile(null);setRequest(null);setError('Üretici profili sunucudan doğrulanamadı.');return;}setProfile(p);setRequest(r&&typeof r==='object'&&!Array.isArray(r)?r:null);setForm({displayName:String(p.display_name||''),description:String(p.description||''),story:String(p.story||'')});setLocation({countryCode:String(p.production_country_code||'').trim().toUpperCase(),province:String(p.production_province||''),district:String(p.production_district||''),village:String(p.production_village||''),villageIsCustom:false,reason:''});}catch(e:any){setError(e?.message||'Üretici profili yüklenemedi.');}finally{if(!silent)setLoading(false);}}
 useEffect(()=>{void load();},[]);
 async function save(e:React.FormEvent){e.preventDefault();if(busy)return;const producerId=normalizedId(profile?.id),displayName=String(form.displayName||'').trim().replace(/\s+/g,' '),description=String(form.description||'').trim(),story=String(form.story||'').trim();if(!producerId){setError('Üretici kimliği doğrulanamadı.');return;}if(displayName.length<2||displayName.length>120){setError('Üretici adı 2 ile 120 karakter arasında olmalıdır.');return;}if(description.length>1200){setError('Kısa açıklama en fazla 1200 karakter olabilir.');return;}if(story.length>5000){setError('Üretici hikâyesi en fazla 5000 karakter olabilir.');return;}try{setBusy(true);setError('');setMessage('');await rpc('update_my_producer_profile_v2',{p_display_name:displayName,p_description:description||null,p_story:story||null,p_logo_path:String(profile?.logo_path||'').trim()||null,p_cover_path:String(profile?.cover_path||'').trim()||null});setMessage('Mağaza profiliniz güncellendi.');await load(true);await onChanged?.();}catch(e:any){setError(e?.message||'Mağaza profili güncellenemedi.');}finally{setBusy(false);}}
 async function requestLocation(e:React.FormEvent){e.preventDefault();if(busy)return;const countryCode=location.countryCode.trim().toUpperCase(),province=location.province.trim(),district=location.district.trim(),village=location.village.trim(),reason=location.reason.trim();if(!/^[A-Z]{2}$/.test(countryCode)){setError('Ülke kodunu TR, CH veya DE gibi iki harfli ISO koduyla yazın.');return;}if(province.length<2||province.length>100||district.length<2||district.length>100){setError('İl/bölge ve ilçe/şehir bilgileri 2 ile 100 karakter arasında olmalıdır.');return;}if(village.length<2||village.length>160){setError('Köy veya mezra adı 2 ile 160 karakter arasında olmalıdır.');return;}if(reason.length<20||reason.length>2000){setError('Konum değişikliği nedenini 20 ile 2000 karakter arasında açıklayın.');return;}try{setBusy(true);setError('');setMessage('');await rpc('request_producer_location_change_v1',{p_country_code:countryCode,p_province:province,p_district:district,p_village:village,p_village_is_custom:location.villageIsCustom,p_latitude:null,p_longitude:null,p_reason:reason});setMessage('Köy/menşe değişikliği admin incelemesine gönderildi.');await load(true);}catch(e:any){setError(e?.message||'Konum değişikliği talebi gönderilemedi.');}finally{setBusy(false);}}
 if(loading)return<LoadingState label="Üretici profili yükleniyor"/>;
 if(!profile)return<ErrorState message={error||'Üretici profili bulunamadı.'} onRetry={()=>void load()}/>;
 const producerId=normalizedId(profile.id),requestStatus=String(request?.status||'').trim(),activeLocationRequest=activeLocationStatuses.has(requestStatus),originVerified=profile?.origin_verified===true;
 return<div className="space-y-5">{error?<ErrorState message={error}/>:null}{message?<div role="status" aria-live="polite" className="rounded-xl bg-green-50 p-3 text-sm text-green-800 dark:bg-green-950/30 dark:text-green-200">{message}</div>:null}
  {producerId?<StoreBrandingEditor producerId={producerId} title="Mağaza logo ve kapak görselleri" description="Mağaza sahibi yalnız kendi mağazasının marka görsellerini yönetebilir. Yeni dosyalar gerçek dosya tipi, piksel ölçüsü ve Storage bütünlüğü doğrulamasından sonra yayın kimliğine bağlanır." onChanged={async()=>{await load(true);await onChanged?.();}}/>:<ErrorState message="Mağaza kimliği doğrulanamadı."/>}
  <Panel title="Mağaza Profilini Düzenle" description="Mağaza adı, kısa açıklama ve hikâye burada güncellenir. Logo ve kapak yukarıdaki güvenli marka editöründen yönetilir; köy ve menşe değişikliği ayrı incelemeye tabidir."><form onSubmit={save} className="space-y-4" aria-busy={busy}><label className="block"><span className="text-sm font-semibold">Mağaza/üretici adı</span><input required minLength={2} maxLength={120} disabled={busy} value={form.displayName} onChange={e=>setForm({...form,displayName:e.target.value})} className="mt-1 min-h-11 w-full rounded-xl border bg-transparent px-3 disabled:opacity-60"/></label><label className="block"><span className="text-sm font-semibold">Kısa açıklama</span><textarea maxLength={1200} disabled={busy} rows={4} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-1 w-full rounded-xl border bg-transparent p-3 disabled:opacity-60"/></label><label className="block"><span className="text-sm font-semibold">Üretici hikâyesi</span><textarea maxLength={5000} disabled={busy} rows={6} value={form.story} onChange={e=>setForm({...form,story:e.target.value})} className="mt-1 w-full rounded-xl border bg-transparent p-3 disabled:opacity-60"/></label><button disabled={busy||form.displayName.trim().length<2} className="min-h-11 w-full rounded-xl bg-brand-green font-bold text-brand-on-green disabled:opacity-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-gold">{busy?'Kaydediliyor…':'Mağaza profilini kaydet'}</button></form></Panel>
  <Panel title="Köy / Menşe Kaydı" description="Bu bilgi müşteri güven rozetlerini etkilediği için satıcı tarafından doğrudan değiştirilemez."><div className="rounded-xl border p-4"><div className="flex flex-wrap items-center gap-2"><div className="font-bold"><MapPin aria-hidden="true" className="mr-2 inline h-4 w-4"/>{String(profile?.production_village||profile?.production_location||'').trim()||'Köy/menşe kaydı yayınlanmadı'}</div>{originVerified?<span className="inline-flex items-center gap-1 rounded-full bg-green-100 px-2.5 py-1 text-xs font-bold text-green-800 dark:bg-green-950/50 dark:text-green-100"><CheckCircle2 aria-hidden="true" className="h-3.5 w-3.5"/>Menşe doğrulandı</span>:<span className="rounded-full bg-gray-100 px-2.5 py-1 text-xs font-bold text-gray-700 dark:bg-gray-800 dark:text-gray-200">Menşe doğrulaması tamamlanmadı</span>}</div>{profile?.production_district||profile?.production_province?<p className="mt-2 text-sm text-gray-500">{[profile.production_district,profile.production_province,profile.production_country_code].map((value:any)=>String(value||'').trim()).filter(Boolean).join(' / ')}</p>:null}</div>
   {activeLocationRequest?<div className="mt-4 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900 dark:border-amber-900/60 dark:bg-amber-950/30 dark:text-amber-100"><strong>Bekleyen konum değişikliği:</strong> {[request?.village,request?.district,request?.province].map((value:any)=>String(value||'').trim()).filter(Boolean).join(' / ')||'Konum ayrıntıları doğrulanamadı'}. Yönetim incelemesi bekleniyor.</div>:<form onSubmit={requestLocation} className="mt-4 space-y-3" aria-busy={busy}><div className="grid gap-3 sm:grid-cols-2"><Field label="Ülke kodu"><input required maxLength={2} disabled={busy} value={location.countryCode} onChange={e=>setLocation({...location,countryCode:e.target.value.toUpperCase().slice(0,2)})}/></Field><Field label="İl/Bölge"><input required minLength={2} maxLength={100} disabled={busy} value={location.province} onChange={e=>setLocation({...location,province:e.target.value})}/></Field><Field label="İlçe/Şehir"><input required minLength={2} maxLength={100} disabled={busy} value={location.district} onChange={e=>setLocation({...location,district:e.target.value})}/></Field><Field label="Köy / mezra"><input required minLength={2} maxLength={160} disabled={busy} value={location.village} onChange={e=>setLocation({...location,village:e.target.value})}/></Field></div><label className="flex min-h-11 items-center gap-3 rounded-xl border p-3"><input type="checkbox" disabled={busy} checked={location.villageIsCustom} onChange={e=>setLocation({...location,villageIsCustom:e.target.checked})} className="h-5 w-5"/><span>Köy/mezra adını listede olmadığı için manuel girdim</span></label><label className="block"><span className="text-sm font-semibold">Değişiklik nedeni</span><textarea required minLength={20} maxLength={2000} disabled={busy} rows={4} value={location.reason} onChange={e=>setLocation({...location,reason:e.target.value})} className="mt-1 w-full rounded-xl border bg-transparent p-3 disabled:opacity-60" placeholder="Taşınma, üretim yerinin değişmesi veya doğrulanmış kayıt düzeltmesi gibi nedeni açıklayın."/><span className="mt-1 block text-xs text-gray-500">{location.reason.trim().length}/2000 karakter • en az 20 karakter</span></label><button disabled={busy||location.reason.trim().length<20} className="min-h-11 w-full rounded-xl border border-brand-gold font-bold text-brand-gold disabled:opacity-50 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-gold">{busy?'Gönderiliyor…':'Konum değişikliğini incelemeye gönder'}</button></form>}
  </Panel>
 </div>;
}
function Field({label,children}:{label:string;children:React.ReactNode}){return<label className="block"><span className="text-sm font-semibold">{label}</span>{React.cloneElement(children as React.ReactElement<any>,{className:'mt-1 min-h-11 w-full rounded-xl border bg-transparent px-3 disabled:opacity-60'})}</label>;}
