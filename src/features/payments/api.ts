import { supabase } from '../../lib/supabase';

export type SavedPaymentMethod={id:string;provider:string;brand:string;last4:string;expMonth:number|null;expYear:number|null;billingName:string|null;nickname:string|null;billingCountryCode:string|null;billingPostalCode:string|null;isDefault:boolean;status:'active'|'expired';createdAt:string;};
export type PaymentReadiness={mode:string;liveCardPaymentsEnabled:boolean;provider:string|null;savedPaymentMethodsSupported:boolean;cardEnrollmentEnabled:boolean;cardEnrollmentMode:string;vaultEdgeFunction:'payment-method-vault';providerHostedCardEntryRequired:false;requiresProviderConfiguration:boolean;paymentVerificationRequired:true;storesProviderSecretsClientSide:false;storesRawCardData:false;storesCvv:false;};
export type PaymentMethodEditableMetadata={nickname?:string|null;billingName?:string|null;billingCountryCode?:string|null;billingPostalCode?:string|null;};
export type CardEnrollmentInput={cardNumber:string;cardHolderName:string;expMonth:number;expYear:number;nickname?:string|null;billingCountryCode?:string|null;billingPostalCode?:string|null;makeDefault?:boolean;consentToSaveCard:boolean;};
function isRecord(value:unknown):value is Record<string,any>{return Boolean(value)&&typeof value==='object'&&!Array.isArray(value);}function requiredText(value:unknown,label:string,max:number){const n=typeof value==='string'?value.trim():'';if(!n||n.length>max||/[\u0000-\u001F\u007F]/.test(n))throw new Error(`${label} doğrulanamadı.`);return n;}function optionalText(value:unknown,max:number){if(value==null||value==='')return null;return requiredText(value,'Ödeme yöntemi metni',max);}function optionalCountryCode(value:unknown){if(value==null||value==='')return null;const n=requiredText(value,'Fatura ülke kodu',2).toUpperCase();if(!/^[A-Z]{2}$/.test(n))throw new Error('Fatura ülke kodu iki harfli ISO kodu olmalıdır.');return n;}function uuid(value:unknown,label:string){const id=requiredText(value,label,160);if(!/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(id))throw new Error(`${label} doğrulanamadı.`);return id;}function optionalInteger(value:unknown,label:string,min:number,max:number){if(value==null)return null;if(typeof value!=='number'||!Number.isSafeInteger(value)||value<min||value>max)throw new Error(`${label} doğrulanamadı.`);return value;}function luhn(cardNumber:string){let sum=0,doubleDigit=false;for(let i=cardNumber.length-1;i>=0;i--){let digit=Number(cardNumber[i]);if(doubleDigit){digit*=2;if(digit>9)digit-=9;}sum+=digit;doubleDigit=!doubleDigit;}return sum%10===0;}function normalizedCardNumber(value:unknown){const digits=typeof value==='string'?value.replace(/[^0-9]/g,''):'';if(digits.length<12||digits.length>19||!luhn(digits))throw new Error('Kart numarası doğrulanamadı.');return digits;}
function normalizeMethod(value:unknown,index:number):SavedPaymentMethod{if(!isRecord(value))throw new Error(`${index+1}. ödeme yöntemi doğrulanamadı.`);const status=requiredText(value.status,'Ödeme yöntemi durumu',20);if(status!=='active'&&status!=='expired')throw new Error('Ödeme yöntemi durumu doğrulanamadı.');const last4=requiredText(value.last4,'Kart son dört hanesi',4);if(!/^[0-9]{4}$/.test(last4))throw new Error('Kart son dört hanesi doğrulanamadı.');const provider=requiredText(value.provider,'Ödeme sağlayıcısı',40).toLowerCase();if(provider!=='iyzico')throw new Error('Ödeme sağlayıcısı doğrulanamadı.');const createdAt=requiredText(value.createdAt,'Ödeme yöntemi kayıt tarihi',80);if(Number.isNaN(Date.parse(createdAt))||typeof value.isDefault!=='boolean')throw new Error('Ödeme yöntemi kayıt bilgisi doğrulanamadı.');return{id:uuid(value.id,'Ödeme yöntemi kimliği'),provider,brand:requiredText(value.brand,'Kart markası',40),last4,expMonth:optionalInteger(value.expMonth,'Son kullanma ayı',1,12),expYear:optionalInteger(value.expYear,'Son kullanma yılı',2024,2200),billingName:optionalText(value.billingName,120),nickname:optionalText(value.nickname,40),billingCountryCode:optionalCountryCode(value.billingCountryCode),billingPostalCode:optionalText(value.billingPostalCode,30),isDefault:value.isDefault,status,createdAt};}
function normalizeReadiness(value:unknown):PaymentReadiness{if(!isRecord(value))throw new Error('Ödeme hazırlık durumu doğrulanamadı.');const provider=value.provider==null?null:requiredText(value.provider,'Ödeme sağlayıcısı',40).toLowerCase(),mode=requiredText(value.mode,'Ödeme modu',60),cardEnrollmentMode=requiredText(value.cardEnrollmentMode,'Kart kayıt modu',80),vault=requiredText(value.vaultEdgeFunction,'Kart kasası fonksiyonu',80);if(vault!=='payment-method-vault'||typeof value.liveCardPaymentsEnabled!=='boolean'||typeof value.savedPaymentMethodsSupported!=='boolean'||typeof value.cardEnrollmentEnabled!=='boolean'||value.providerHostedCardEntryRequired!==false||typeof value.requiresProviderConfiguration!=='boolean'||value.paymentVerificationRequired!==true||value.storesProviderSecretsClientSide!==false||value.storesRawCardData!==false||value.storesCvv!==false)throw new Error('Ödeme güvenlik sözleşmesi doğrulanamadı.');if((value.liveCardPaymentsEnabled||value.cardEnrollmentEnabled)&&provider!=='iyzico')throw new Error('Ödeme sağlayıcısı açıkken iyzico yapılandırması eksik.');if(value.cardEnrollmentEnabled&&!value.savedPaymentMethodsSupported)throw new Error('Kart kayıt özelliği kayıtlı ödeme yöntemi desteği olmadan açılamaz.');return{mode,liveCardPaymentsEnabled:value.liveCardPaymentsEnabled,provider,savedPaymentMethodsSupported:value.savedPaymentMethodsSupported,cardEnrollmentEnabled:value.cardEnrollmentEnabled,cardEnrollmentMode,vaultEdgeFunction:'payment-method-vault',providerHostedCardEntryRequired:false,requiresProviderConfiguration:value.requiresProviderConfiguration,paymentVerificationRequired:true,storesProviderSecretsClientSide:false,storesRawCardData:false,storesCvv:false};}
function providerError(error:any,data:unknown,fallback:string){const detail=isRecord(data)&&typeof data.error==='string'?data.error:'',raw=detail||String(error?.message||fallback);if(raw.includes('payment_provider_not_configured')||raw.includes('credentials_missing'))return new Error('iyzico kart kaydı için henüz yapılandırılmadı.');if(raw.includes('card_storage_consent_required'))return new Error('Kartı kaydetmek için açık onay vermeniz gerekir.');if(raw.includes('invalid_card_number'))return new Error('Kart numarası doğrulanamadı.');if(raw.includes('card_expired'))return new Error('Kartın son kullanma tarihi geçmiş.');if(raw.includes('invalid_card_holder_name'))return new Error('Kart sahibi adı doğrulanamadı.');if(raw.includes('payment_method_not_found'))return new Error('Kayıtlı kart bulunamadı veya artık kullanılamıyor.');if(raw.includes('payment_provider_error'))return new Error('iyzico kart işlemini kabul etmedi. Kart bilgilerini kontrol edin veya başka bir kart deneyin.');return new Error(fallback);}
export async function listMyPaymentMethods():Promise<SavedPaymentMethod[]>{const{data,error}=await supabase.rpc('list_my_payment_methods_v1');if(error)throw error;if(!Array.isArray(data)||data.length>20)throw new Error('Ödeme yöntemleri sunucudan doğrulanamadı.');return data.map(normalizeMethod);}
export async function getPaymentReadiness():Promise<PaymentReadiness>{const{data,error}=await supabase.rpc('get_checkout_payment_readiness_v3');if(error)throw error;return normalizeReadiness(data);}
export async function enrollMyPaymentMethod(input:CardEnrollmentInput):Promise<SavedPaymentMethod>{if(input.consentToSaveCard!==true)throw new Error('Kartı kaydetmek için açık onay vermeniz gerekir.');const cardNumber=normalizedCardNumber(input.cardNumber),cardHolderName=requiredText(input.cardHolderName,'Kart sahibi adı',120);if(cardHolderName.length<2)throw new Error('Kart sahibi adı en az 2 karakter olmalıdır.');if(!Number.isSafeInteger(input.expMonth)||input.expMonth<1||input.expMonth>12)throw new Error('Son kullanma ayı doğrulanamadı.');const currentYear=new Date().getUTCFullYear();if(!Number.isSafeInteger(input.expYear)||input.expYear<currentYear||input.expYear>2200)throw new Error('Son kullanma yılı doğrulanamadı.');const{data,error}=await supabase.functions.invoke('payment-method-vault',{body:{action:'add',cardNumber,cardHolderName,expMonth:input.expMonth,expYear:input.expYear,nickname:optionalText(input.nickname,40),billingCountryCode:optionalCountryCode(input.billingCountryCode),billingPostalCode:optionalText(input.billingPostalCode,30),makeDefault:input.makeDefault===true,consentToSaveCard:true}});if(error||!isRecord(data)||data.ok!==true||!isRecord(data.method))throw providerError(error,data,'Kart kaydedilemedi.');const methodId=uuid(data.method.id,'Ödeme yöntemi kimliği'),methods=await listMyPaymentMethods(),saved=methods.find(method=>method.id===methodId);if(!saved)throw new Error('Kart iyzico üzerinde kaydedildi ancak hesap listesinden doğrulanamadı.');return saved;}
export async function setMyDefaultPaymentMethod(paymentMethodId:string){const id=uuid(paymentMethodId,'Ödeme yöntemi kimliği');const{data,error}=await supabase.rpc('set_my_default_payment_method_v1',{p_payment_method_id:id});if(error)throw error;if(!isRecord(data)||data.ok!==true||data.isDefault!==true||uuid(data.id,'Ödeme yöntemi kimliği')!==id)throw new Error('Varsayılan ödeme yöntemi sonucu doğrulanamadı.');return data;}
export async function updateMyPaymentMethodMetadata(paymentMethodId:string,input:PaymentMethodEditableMetadata){const id=uuid(paymentMethodId,'Ödeme yöntemi kimliği'),nickname=optionalText(input.nickname,40),billingName=optionalText(input.billingName,120),billingCountryCode=optionalCountryCode(input.billingCountryCode),billingPostalCode=optionalText(input.billingPostalCode,30);const{data,error}=await supabase.rpc('update_my_payment_method_metadata_v1',{p_payment_method_id:id,p_nickname:nickname,p_billing_name:billingName,p_billing_country_code:billingCountryCode,p_billing_postal_code:billingPostalCode});if(error)throw error;if(!isRecord(data)||data.ok!==true||uuid(data.id,'Ödeme yöntemi kimliği')!==id)throw new Error('Ödeme yöntemi güncelleme sonucu doğrulanamadı.');return{ok:true as const,id,nickname:data.nickname==null?null:optionalText(data.nickname,40),billingName:data.billingName==null?null:optionalText(data.billingName,120),billingCountryCode:data.billingCountryCode==null?null:optionalCountryCode(data.billingCountryCode),billingPostalCode:data.billingPostalCode==null?null:optionalText(data.billingPostalCode,30)};}
export async function removeMyPaymentMethod(paymentMethodId:string){const id=uuid(paymentMethodId,'Ödeme yöntemi kimliği');const{data,error}=await supabase.functions.invoke('payment-method-vault',{body:{action:'remove',paymentMethodId:id}});if(error||!isRecord(data)||data.ok!==true)throw providerError(error,data,'Kayıtlı kart kaldırılamadı.');const removedId=uuid(data.removedId,'Silinen ödeme yöntemi kimliği');if(removedId!==id)throw new Error('Silinen ödeme yöntemi sonucu istekle eşleşmiyor.');return{ok:true as const,removedId,newDefaultId:data.newDefaultId==null?null:uuid(data.newDefaultId,'Yeni varsayılan ödeme yöntemi kimliği')};}
export function paymentMethodLabel(method:SavedPaymentMethod){const expiry=method.expMonth&&method.expYear?` • ${String(method.expMonth).padStart(2,'0')}/${String(method.expYear).slice(-2)}`:'',card=`${method.brand} •••• ${method.last4}${expiry}`;return method.nickname?.trim()?`${method.nickname.trim()} • ${card}`:card;}
