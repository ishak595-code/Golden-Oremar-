import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2.111.0";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type, x-golden-device-id","Access-Control-Allow-Methods":"POST, OPTIONS"};
const UUID_RE=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const PRODUCER_PATH_RE=/^([0-9a-f-]{36})\/products\/([0-9a-f-]{36})\.(jpg|jpeg|png|webp|avif)$/i;
const ADMIN_PATH_RE=/^admin\/([0-9a-f-]{36})\/official-products\/([0-9a-f-]{36})\.(jpg|jpeg|png|webp|avif)$/i;
const MAX_BYTES=10*1024*1024;
function json(status:number,body:Record<string,unknown>){return new Response(JSON.stringify(body),{status,headers:{...cors,"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"}});}
function text(value:unknown,max=1200){return typeof value==='string'?value.trim().slice(0,max):'';}
function ascii(bytes:Uint8Array,start:number,length:number){return String.fromCharCode(...bytes.slice(start,start+length));}
function equal(bytes:Uint8Array,signature:number[],offset=0){return signature.every((value,index)=>bytes[offset+index]===value);}
function detectMime(bytes:Uint8Array){if(bytes.length>=3&&equal(bytes,[0xff,0xd8,0xff]))return'image/jpeg';if(bytes.length>=24&&equal(bytes,[0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a])&&ascii(bytes,12,4)==='IHDR')return'image/png';if(bytes.length>=12&&ascii(bytes,0,4)==='RIFF'&&ascii(bytes,8,4)==='WEBP')return'image/webp';if(bytes.length>=16&&ascii(bytes,4,4)==='ftyp'){const brands:string[]=[ascii(bytes,8,4)];const limit=Math.min(bytes.length,64);for(let offset=16;offset+4<=limit;offset+=4)brands.push(ascii(bytes,offset,4));if(brands.includes('avif')||brands.includes('avis'))return'image/avif';}return'';}
function extensionMatches(path:string,mime:string){const lower=path.toLowerCase();if(mime==='image/jpeg')return/\.(jpg|jpeg)$/.test(lower);if(mime==='image/png')return lower.endsWith('.png');if(mime==='image/webp')return lower.endsWith('.webp');if(mime==='image/avif')return lower.endsWith('.avif');return false;}
async function sha256Hex(bytes:Uint8Array){const digest=await crypto.subtle.digest('SHA-256',bytes);return Array.from(new Uint8Array(digest),value=>value.toString(16).padStart(2,'0')).join('');}
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response(null,{status:204,headers:cors});if(req.method!=='POST')return json(405,{ok:false,error:'method_not_allowed'});try{const url=Deno.env.get('SUPABASE_URL')||'',anon=Deno.env.get('SUPABASE_ANON_KEY')||'',serviceKey=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'',authorization=req.headers.get('Authorization')||'';if(!url||!anon||!serviceKey||!authorization)return json(401,{ok:false,error:'authentication_required'});const userClient=createClient(url,anon,{global:{headers:{Authorization:authorization}},auth:{persistSession:false,autoRefreshToken:false}}),service=createClient(url,serviceKey,{auth:{persistSession:false,autoRefreshToken:false}});const{data:userData,error:userError}=await userClient.auth.getUser();const userId=userData.user?.id||'';if(userError||!UUID_RE.test(userId))return json(401,{ok:false,error:'authentication_required'});const body=await req.json().catch(()=>null) as Record<string,unknown>|null;const path=text(body?.path);if(!path||path.startsWith('/')||path.split('/').some(part=>!part||part==='.'||part==='..')||/^[a-z][a-z0-9+.-]*:/i.test(path))return json(400,{ok:false,error:'catalog_media_path_invalid'});const producerMatch=path.match(PRODUCER_PATH_RE),adminMatch=path.match(ADMIN_PATH_RE);if(producerMatch){const producerId=producerMatch[1];if(!UUID_RE.test(producerId))return json(403,{ok:false,error:'catalog_media_owner_invalid'});const{data:producer,error:producerError}=await service.from('producers').select('id,owner_user_id,status,deleted_at').eq('id',producerId).eq('owner_user_id',userId).eq('status','active').is('deleted_at',null).maybeSingle();if(producerError||!producer)return json(403,{ok:false,error:'catalog_media_owner_required'});}else if(adminMatch){if(adminMatch[1].toLowerCase()!==userId.toLowerCase())return json(403,{ok:false,error:'catalog_media_owner_required'});const[{data:adminAccess,error:adminError},{data:updateAccess,error:updateError}]=await Promise.all([userClient.rpc('authorization_has_permission_v1',{p_permission_key:'admin.access'}),userClient.rpc('authorization_has_permission_v1',{p_permission_key:'product.update'})]);if(adminError||updateError||adminAccess!==true||updateAccess!==true)return json(403,{ok:false,error:'catalog_media_management_permission_required'});}else return json(403,{ok:false,error:'catalog_media_namespace_invalid'});const{data:blob,error:downloadError}=await service.storage.from('catalog-public').download(path);if(downloadError||!blob)return json(404,{ok:false,error:'catalog_media_object_missing'});if(blob.size<1||blob.size>MAX_BYTES)return json(400,{ok:false,error:'catalog_media_size_invalid'});const bytes=new Uint8Array(await blob.arrayBuffer());if(bytes.byteLength!==blob.size)return json(400,{ok:false,error:'catalog_media_size_mismatch'});const detectedMime=detectMime(bytes);if(!detectedMime)return json(400,{ok:false,error:'catalog_media_binary_type_invalid'});if(!extensionMatches(path,detectedMime))return json(400,{ok:false,error:'catalog_media_extension_mismatch'});const checksum=await sha256Hex(bytes);const{data:recorded,error:recordError}=await service.rpc('catalog_media_record_binary_verification_v2',{p_path:path,p_detected_mime:detectedMime,p_byte_size:bytes.byteLength,p_sha256:checksum,p_verified_by:userId});if(recordError||!recorded||recorded.ok!==true)throw recordError||new Error('catalog_media_verification_record_failed');return json(200,{ok:true,path,detectedMime,byteSize:bytes.byteLength});}catch(error){const message=error instanceof Error?error.message:'catalog_media_verification_failed';return json(400,{ok:false,error:message.length<=200?message:'catalog_media_verification_failed'});}});
