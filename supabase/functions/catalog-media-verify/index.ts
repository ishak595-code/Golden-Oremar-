import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2.111.0";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type, x-golden-device-id","Access-Control-Allow-Methods":"POST, OPTIONS"};
const UUID='[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}';
const UUID_RE=new RegExp(`^${UUID}$`,'i');
const PRODUCER_PATH_RE=new RegExp(`^(${UUID})/products/(${UUID})[.](jpg|jpeg|png|webp|avif)$`,'i');
const ADMIN_PATH_RE=new RegExp(`^admin/(${UUID})/official-products/(${UUID})[.](jpg|jpeg|png|webp|avif)$`,'i');
const BRAND_PATH_RE=new RegExp(`^(${UUID})/profile/(logo|cover)-(${UUID})[.](jpg|jpeg|png|webp)$`,'i');
const MAX_BYTES=10*1024*1024,BRAND_MAX_BYTES=5*1024*1024;
function json(status:number,body:Record<string,unknown>){return new Response(JSON.stringify(body),{status,headers:{...cors,"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"}});}
function text(value:unknown,max=1200){return typeof value==='string'?value.trim().slice(0,max):'';}
function ascii(bytes:Uint8Array,start:number,length:number){return String.fromCharCode(...bytes.slice(start,start+length));}
function equal(bytes:Uint8Array,signature:number[],offset=0){return signature.every((value,index)=>bytes[offset+index]===value);}
function detectMime(bytes:Uint8Array){if(bytes.length>=3&&equal(bytes,[0xff,0xd8,0xff]))return'image/jpeg';if(bytes.length>=24&&equal(bytes,[0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a])&&ascii(bytes,12,4)==='IHDR')return'image/png';if(bytes.length>=12&&ascii(bytes,0,4)==='RIFF'&&ascii(bytes,8,4)==='WEBP')return'image/webp';if(bytes.length>=16&&ascii(bytes,4,4)==='ftyp'){const brands:string[]=[ascii(bytes,8,4)];const limit=Math.min(bytes.length,64);for(let offset=16;offset+4<=limit;offset+=4)brands.push(ascii(bytes,offset,4));if(brands.includes('avif')||brands.includes('avis'))return'image/avif';}return'';}
function extensionMatches(path:string,mime:string){const lower=path.toLowerCase();if(mime==='image/jpeg')return/[.](jpg|jpeg)$/.test(lower);if(mime==='image/png')return lower.endsWith('.png');if(mime==='image/webp')return lower.endsWith('.webp');if(mime==='image/avif')return lower.endsWith('.avif');return false;}
function pngDimensions(bytes:Uint8Array){if(bytes.length<24)return null;const view=new DataView(bytes.buffer,bytes.byteOffset,bytes.byteLength);return{width:view.getUint32(16,false),height:view.getUint32(20,false)};}
const JPEG_SOF=new Set([0xc0,0xc1,0xc2,0xc3,0xc5,0xc6,0xc7,0xc9,0xca,0xcb,0xcd,0xce,0xcf]);
function jpegDimensions(bytes:Uint8Array){let offset=2;while(offset+4<=bytes.length){if(bytes[offset]!==0xff){offset++;continue;}while(offset<bytes.length&&bytes[offset]===0xff)offset++;if(offset>=bytes.length)break;const marker=bytes[offset++];if(marker===0xd9||marker===0xda)break;if(marker===0xd8||marker===0x01||(marker>=0xd0&&marker<=0xd7))continue;if(offset+2>bytes.length)break;const length=(bytes[offset]<<8)|bytes[offset+1];if(length<2||offset+length>bytes.length)break;if(JPEG_SOF.has(marker)&&length>=7){const height=(bytes[offset+3]<<8)|bytes[offset+4],width=(bytes[offset+5]<<8)|bytes[offset+6];return width&&height?{width,height}:null;}offset+=length;}return null;}
function uint24le(bytes:Uint8Array,offset:number){return bytes[offset]|(bytes[offset+1]<<8)|(bytes[offset+2]<<16);}
function webpDimensions(bytes:Uint8Array){if(bytes.length<30)return null;const chunk=ascii(bytes,12,4);if(chunk==='VP8X')return{width:uint24le(bytes,24)+1,height:uint24le(bytes,27)+1};if(chunk==='VP8L'&&bytes[20]===0x2f){const b0=bytes[21],b1=bytes[22],b2=bytes[23],b3=bytes[24];return{width:1+(b0|((b1&0x3f)<<8)),height:1+(((b1&0xc0)>>6)|(b2<<2)|((b3&0x0f)<<10))};}if(chunk==='VP8 '&&bytes[23]===0x9d&&bytes[24]===0x01&&bytes[25]===0x2a){const width=(bytes[26]|(bytes[27]<<8))&0x3fff,height=(bytes[28]|(bytes[29]<<8))&0x3fff;return width&&height?{width,height}:null;}return null;}
function imageDimensions(bytes:Uint8Array,mime:string){if(mime==='image/png')return pngDimensions(bytes);if(mime==='image/jpeg')return jpegDimensions(bytes);if(mime==='image/webp')return webpDimensions(bytes);return null;}
function brandDimensionsValid(kind:string,width:number,height:number){if(!Number.isSafeInteger(width)||!Number.isSafeInteger(height)||width<1||height<1)return false;if(kind==='logo')return width===height&&width>=512&&width<=4096;if(kind==='cover'){const ratio=width/height;return width>=1200&&height>=480&&width<=6000&&height<=2400&&Math.abs(ratio-2.5)<=0.025;}return false;}
async function sha256Hex(bytes:Uint8Array){const digest=await crypto.subtle.digest('SHA-256',bytes);return Array.from(new Uint8Array(digest),value=>value.toString(16).padStart(2,'0')).join('');}
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response(null,{status:204,headers:cors});if(req.method!=='POST')return json(405,{ok:false,error:'method_not_allowed'});try{const url=Deno.env.get('SUPABASE_URL')||'',anon=Deno.env.get('SUPABASE_ANON_KEY')||'',serviceKey=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'',authorization=req.headers.get('Authorization')||'';if(!url||!anon||!serviceKey||!authorization)return json(401,{ok:false,error:'authentication_required'});const userClient=createClient(url,anon,{global:{headers:{Authorization:authorization}},auth:{persistSession:false,autoRefreshToken:false}}),service=createClient(url,serviceKey,{auth:{persistSession:false,autoRefreshToken:false}});const{data:userData,error:userError}=await userClient.auth.getUser();const userId=userData.user?.id||'';if(userError||!UUID_RE.test(userId))return json(401,{ok:false,error:'authentication_required'});const body=await req.json().catch(()=>null) as Record<string,unknown>|null;const path=text(body?.path);if(!path||path.startsWith('/')||path.split('/').some(part=>!part||part==='.'||part==='..')||/^[a-z][a-z0-9+.-]*:/i.test(path))return json(400,{ok:false,error:'catalog_media_path_invalid'});const producerMatch=path.match(PRODUCER_PATH_RE),adminMatch=path.match(ADMIN_PATH_RE),brandMatch=path.match(BRAND_PATH_RE);let brandKind='';if(producerMatch){const producerId=producerMatch[1];const{data:producer,error:producerError}=await service.from('producers').select('id,owner_user_id,status,is_verified,origin_verified,deleted_at').eq('id',producerId).eq('owner_user_id',userId).eq('status','active').eq('is_verified',true).eq('origin_verified',true).is('deleted_at',null).maybeSingle();if(producerError||!producer)return json(403,{ok:false,error:'catalog_media_owner_required'});}else if(adminMatch){if(adminMatch[1].toLowerCase()!==userId.toLowerCase())return json(403,{ok:false,error:'catalog_media_owner_required'});const[{data:adminAccess,error:adminError},{data:publishAccess,error:publishError}]=await Promise.all([userClient.rpc('authorization_has_permission_v1',{p_permission_key:'admin.access'}),userClient.rpc('authorization_has_permission_v1',{p_permission_key:'product.publish'})]);if(adminError||publishError||adminAccess!==true||publishAccess!==true)return json(403,{ok:false,error:'catalog_media_owner_permission_required'});}else if(brandMatch){const producerId=brandMatch[1],kind=brandMatch[2].toLowerCase();brandKind=kind;const{data:producer,error:producerError}=await service.from('producers').select('id,owner_user_id,status,is_verified,store_kind,deleted_at').eq('id',producerId).is('deleted_at',null).maybeSingle();if(producerError||!producer||producer.is_verified!==true)return json(403,{ok:false,error:'store_branding_owner_required'});let allowed=producer.store_kind!=='official'&&['pending','active'].includes(String(producer.status||''))&&String(producer.owner_user_id||'').toLowerCase()===userId.toLowerCase();if(producer.store_kind==='official'){const{data:publishAccess,error:publishError}=await userClient.rpc('authorization_has_permission_v1',{p_permission_key:'product.publish'});allowed=producer.status==='active'&&!publishError&&publishAccess===true;}if(!allowed)return json(403,{ok:false,error:'store_branding_owner_required'});}else return json(403,{ok:false,error:'catalog_media_namespace_invalid'});const{data:blob,error:downloadError}=await service.storage.from('catalog-public').download(path);if(downloadError||!blob)return json(404,{ok:false,error:'catalog_media_object_missing'});const maxBytes=brandMatch?BRAND_MAX_BYTES:MAX_BYTES;if(blob.size<1||blob.size>maxBytes)return json(400,{ok:false,error:brandMatch?'store_branding_size_invalid':'catalog_media_size_invalid'});const bytes=new Uint8Array(await blob.arrayBuffer());if(bytes.byteLength!==blob.size)return json(400,{ok:false,error:'catalog_media_size_mismatch'});const detectedMime=detectMime(bytes);if(!detectedMime)return json(400,{ok:false,error:'catalog_media_binary_type_invalid'});if(brandMatch&&detectedMime==='image/avif')return json(400,{ok:false,error:'store_branding_type_invalid'});if(!extensionMatches(path,detectedMime))return json(400,{ok:false,error:'catalog_media_extension_mismatch'});let dimensions:{width:number;height:number}|null=null;if(brandMatch){dimensions=imageDimensions(bytes,detectedMime);if(!dimensions)return json(400,{ok:false,error:'store_branding_dimensions_unreadable'});if(!brandDimensionsValid(brandKind,dimensions.width,dimensions.height))return json(400,{ok:false,error:brandKind==='logo'?'store_branding_logo_dimensions_invalid':'store_branding_cover_dimensions_invalid',width:dimensions.width,height:dimensions.height});}const checksum=await sha256Hex(bytes);const{data:recorded,error:recordError}=await service.rpc('catalog_media_record_binary_verification_v2',{p_path:path,p_detected_mime:detectedMime,p_byte_size:bytes.byteLength,p_sha256:checksum,p_verified_by:userId});if(recordError||!recorded||recorded.ok!==true)throw recordError||new Error('catalog_media_verification_record_failed');return json(200,{ok:true,path,detectedMime,byteSize:bytes.byteLength,...(dimensions?{width:dimensions.width,height:dimensions.height,assetKind:brandKind}:{})});}catch(error){const message=error instanceof Error?error.message:'catalog_media_verification_failed';return json(400,{ok:false,error:message.length<=200?message:'catalog_media_verification_failed'});}});
