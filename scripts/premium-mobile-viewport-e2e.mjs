import fs from'node:fs';import path from'node:path';import{chromium}from'playwright';
const baseUrl=(process.env.E2E_BASE_URL||'http://127.0.0.1:4173').replace(/\/+$/,'');const widths=[320,360,375,390,412,430];const out=path.resolve('e2e-artifacts');fs.mkdirSync(out,{recursive:true});const results=[];
const browser=await chromium.launch({headless:true});
async function loadAllDeferredSections(page,width){await page.evaluate(()=>{document.documentElement.style.scrollBehavior='auto';document.body.style.scrollBehavior='auto';});const sections=page.locator('.go-product-section-v2--deferred');const count=await sections.count();for(let index=0;index<count;index++){const host=sections.nth(index);await page.evaluate(sectionIndex=>{const hosts=[...document.querySelectorAll('.go-product-section-v2--deferred')];const target=hosts[sectionIndex];if(!target)throw new Error(`DEFERRED_SECTION_HOST_MISSING_${sectionIndex}`);const top=target.getBoundingClientRect().top+window.scrollY-Math.max(80,window.innerHeight*.25);window.scrollTo({top:Math.max(0,top),behavior:'auto'});},index);const firstCard=host.locator('.go-product-card-v2').first();const error=host.locator('.go-inline-error');await Promise.race([firstCard.waitFor({state:'visible',timeout:15000}),error.waitFor({state:'visible',timeout:15000}).then(async()=>{throw new Error(`DEFERRED_SECTION_ERROR_${width}_${index}:${(await error.textContent())||'unknown'}`);})]);}return count;}
async function verifyAdaptiveHeader(page,width){const scrollTarget=await page.evaluate(()=>{document.documentElement.style.scrollBehavior='auto';document.body.style.scrollBehavior='auto';const maxScroll=Math.max(0,document.documentElement.scrollHeight-window.innerHeight);const target=Math.min(240,maxScroll);if(target<=104)throw new Error(`ADAPTIVE_HEADER_SCROLL_RANGE_TOO_SMALL:${target}`);window.scrollTo({top:target,behavior:'auto'});return target;});await page.waitForFunction(()=>document.documentElement.dataset.goHeaderCompact==='true',null,{timeout:2500});const compact=await page.evaluate(()=>({compact:document.documentElement.dataset.goHeaderCompact,scrollY:window.scrollY,searchVisible:!!document.querySelector('.go-search-bar input')?.getBoundingClientRect().width,voiceVisible:!!document.querySelector('.go-search-bar__voice')?.getBoundingClientRect().width,cartVisible:!!document.querySelector('header button[aria-label^="Sepetim"]')?.getBoundingClientRect().width}));if(compact.compact!=='true'||!compact.searchVisible||!compact.voiceVisible||!compact.cartVisible)throw new Error(`ADAPTIVE_HEADER_COMPACT_FAILED_${width}:${JSON.stringify(compact)}`);await page.evaluate(()=>window.scrollTo({top:0,behavior:'auto'}));await page.waitForFunction(()=>document.documentElement.dataset.goHeaderCompact==='false',null,{timeout:2500});return{scrollTarget,...compact,expanded:true};}
async function verifyProductDetailDock(page,width){await page.goto(`${baseUrl}/?tab=home`,{waitUntil:'networkidle',timeout:45000});const row=page.locator('.go-product-card-v2__button').first();await row.waitFor({state:'visible',timeout:15000});if((await row.evaluate(element=>element.tagName))!=='A')throw new Error(`HOME_PRODUCT_ROW_NOT_LINK_${width}`);await row.click();await page.waitForFunction(()=>document.documentElement.dataset.appTab==='product-detail',null,{timeout:15000});const dock=page.locator('.product-detail-commerce-dock');await dock.waitFor({state:'visible',timeout:15000});await page.getByRole('button',{name:'Miktarı azalt',exact:true}).waitFor({state:'visible',timeout:5000});await page.getByRole('button',{name:'Miktarı artır',exact:true}).waitFor({state:'visible',timeout:5000});if(await page.locator('input[type="radio"][name="variant"]').count()!==0)throw new Error(`PRODUCT_DETAIL_REDUNDANT_RADIO_OPTION_${width}`);if(await page.getByText('Paket / seçenek',{exact:true}).count()!==0)throw new Error(`PRODUCT_DETAIL_SINGLE_VARIANT_SELECTOR_VISIBLE_${width}`);const dockText=(await dock.textContent()||'').replace(/\s+/g,' ').trim();if(/\bTL\b/.test(dockText))throw new Error(`PRODUCT_DETAIL_DOCK_REPEATED_PRICE_${width}:${dockText}`);const before=await dock.evaluate(element=>{const rect=element.getBoundingClientRect(),style=getComputedStyle(element),buttons=[...element.querySelectorAll('.product-detail-commerce-actions>button')];return{position:style.position,top:rect.top,bottom:rect.bottom,left:rect.left,right:rect.right,viewportHeight:window.innerHeight,labels:buttons.map(button=>(button.textContent||'').replace(/\s+/g,' ').trim()),visible:buttons.map(button=>{const r=button.getBoundingClientRect();return r.width>0&&r.height>0&&r.top>=-1&&r.bottom<=window.innerHeight+1;})};});if(before.position!=='fixed')throw new Error(`PRODUCT_DETAIL_DOCK_NOT_FIXED_${width}:${before.position}`);if(before.labels.length!==3)throw new Error(`PRODUCT_DETAIL_ACTION_COUNT_${width}:${before.labels.length}`);if(before.labels[0]!=='Hediye Et'||!['Sepete Ekle','Ön Sipariş'].includes(before.labels[1])||before.labels[2]!=='Hemen Satın Al')throw new Error(`PRODUCT_DETAIL_ACTION_ORDER_${width}:${JSON.stringify(before.labels)}`);if(before.visible.some(value=>!value))throw new Error(`PRODUCT_DETAIL_ACTION_NOT_VISIBLE_${width}:${JSON.stringify(before.visible)}`);if(before.left<-1||before.right>width+1||before.bottom>before.viewportHeight+1)throw new Error(`PRODUCT_DETAIL_DOCK_OVERFLOW_${width}:${JSON.stringify(before)}`);await page.evaluate(()=>{document.documentElement.style.scrollBehavior='auto';document.body.style.scrollBehavior='auto';const max=Math.max(0,document.documentElement.scrollHeight-window.innerHeight);window.scrollTo({top:Math.max(0,max*.72),behavior:'auto'});});await page.waitForTimeout(120);const after=await dock.evaluate(element=>{const rect=element.getBoundingClientRect();return{top:rect.top,bottom:rect.bottom,left:rect.left,right:rect.right,viewportHeight:window.innerHeight,scrollY:window.scrollY};});if(after.scrollY<1)throw new Error(`PRODUCT_DETAIL_SCROLL_RANGE_TOO_SMALL_${width}`);if(after.left<-1||after.right>width+1||after.top<-1||after.bottom>after.viewportHeight+1)throw new Error(`PRODUCT_DETAIL_DOCK_LOST_AFTER_SCROLL_${width}:${JSON.stringify(after)}`);if(Math.abs(after.top-before.top)>2||Math.abs(after.bottom-before.bottom)>2)throw new Error(`PRODUCT_DETAIL_DOCK_MOVED_WITH_CONTENT_${width}:${before.top}/${after.top}:${before.bottom}/${after.bottom}`);await page.screenshot({path:path.join(out,`product-detail-fixed-dock-${width}.png`),fullPage:false});return{before,after};}
async function verifyCategoryViewport(page,width){await page.goto(`${baseUrl}/?tab=categories`,{waitUntil:'networkidle',timeout:45000});await page.getByRole('heading',{name:'Lezzetleri kendi ritminizde keşfedin',exact:true}).waitFor({state:'visible',timeout:15000});await page.locator('#category-products article').first().waitFor({state:'visible',timeout:15000});const metrics=await page.evaluate(()=>{const root=document.documentElement,select=document.querySelector('select'),stock=document.querySelector('input[type="checkbox"]')?.closest('label'),grid=document.querySelector('#category-products'),card=document.querySelector('#category-products article');if(!select||!stock||!grid||!card)throw new Error('CATEGORY_VIEWPORT_REQUIRED_ELEMENT_MISSING');const sr=select.getBoundingClientRect(),fr=stock.getBoundingClientRect(),cr=card.getBoundingClientRect(),gridStyle=getComputedStyle(grid);return{scrollWidth:root.scrollWidth,clientWidth:root.clientWidth,selectRight:sr.right,filterRight:fr.right,cardRight:cr.right,selectLeft:sr.left,filterLeft:fr.left,cardLeft:cr.left,productGridGap:Number.parseFloat(gridStyle.rowGap)||Number.parseFloat(gridStyle.gap)||0};});if(metrics.scrollWidth>metrics.clientWidth+1)throw new Error(`CATEGORY_HORIZONTAL_OVERFLOW_${width}:${metrics.scrollWidth}>${metrics.clientWidth}`);for(const [name,left,right]of[['sort',metrics.selectLeft,metrics.selectRight],['stock',metrics.filterLeft,metrics.filterRight],['card',metrics.cardLeft,metrics.cardRight]])if(left<-1||right>width+1)throw new Error(`CATEGORY_COMPONENT_OVERFLOW_${width}_${name}:${left}:${right}`);if(metrics.productGridGap<19)throw new Error(`CATEGORY_PRODUCT_GRID_DENSITY_${width}:${metrics.productGridGap}`);await page.screenshot({path:path.join(out,`category-mobile-${width}.png`),fullPage:true});return metrics;}
try{for(const width of widths){const context=await browser.newContext({locale:'tr-TR',viewport:{width,height:844},deviceScaleFactor:1,isMobile:true,hasTouch:true});const page=await context.newPage();const errors=[];page.on('pageerror',error=>errors.push(String(error?.message||error)));page.on('console',msg=>{if(msg.type()==='error')errors.push(msg.text())});await page.goto(`${baseUrl}/?tab=home`,{waitUntil:'networkidle',timeout:45000});
 const search=page.getByRole('searchbox',{name:'Ürün, üretici veya köy ara',exact:true});if(await search.count()!==1)throw new Error(`SEARCHBOX_ACCESSIBLE_NAME_COUNT_${width}:${await search.count()}`);await search.waitFor({state:'visible',timeout:15000});const voice=page.getByRole('button',{name:'Sesli mikrofon',exact:true});if(await voice.count()!==1)throw new Error(`VOICE_CONTROL_COUNT_${width}:${await voice.count()}`);await voice.waitFor({state:'visible',timeout:5000});if(await page.getByRole('button',{name:/Mikrofon kapalı|Sesli aramayı başlat/i}).count()!==0)throw new Error(`STALE_MICROPHONE_CONTROL_${width}`);if(await page.getByText(/Mikrofon kapalı/i).count()!==0)throw new Error(`STALE_MICROPHONE_OFF_COPY_${width}`);await page.locator('.go-product-card-v2').first().waitFor({state:'visible',timeout:15000});
 const initial=await page.evaluate(()=>{const root=document.documentElement,header=document.querySelector('header'),nav=document.querySelector('nav[aria-label="Ana gezinme"]'),title=document.querySelector('.go-product-card-v2__title'),card=document.querySelector('.go-product-card-v2'),cardButton=document.querySelector('.go-product-card-v2__button'),voice=document.querySelector('.go-search-bar__voice'),search=document.querySelector('.go-search-bar input'),productGrid=document.querySelector('.go-product-grid-v2'),sectionHeader=document.querySelector('.go-section-header'),media=document.querySelector('.go-product-card-v2__media'),categoryCard=document.querySelector('.go-category-card'),sections=[...document.querySelectorAll('.go-home-section')];if(!header||!nav||!title||!card||!cardButton||!voice||!search||!productGrid||!sectionHeader||!media)throw new Error('PREMIUM_HOME_REQUIRED_ELEMENT_MISSING');const titleStyle=getComputedStyle(title),image=document.querySelector('.go-product-card-v2__media img'),cardButtonStyle=getComputedStyle(cardButton),gridStyle=getComputedStyle(productGrid),headerStyle=getComputedStyle(sectionHeader),support=sectionHeader.querySelector('p'),supportStyle=support?getComputedStyle(support):null,spacedSection=sections.length>1?sections[1]:null,cardRect=card.getBoundingClientRect(),mediaRect=media.getBoundingClientRect();return{scrollWidth:root.scrollWidth,clientWidth:root.clientWidth,headerHeight:header.getBoundingClientRect().height,navButtons:nav.querySelectorAll('button').length,titleWhiteSpace:titleStyle.whiteSpace,titleClamp:titleStyle.getPropertyValue('-webkit-line-clamp'),titleLineHeight:titleStyle.lineHeight,cardWidth:cardRect.width,cardLeft:cardRect.left,cardRight:cardRect.right,cardButtonHeight:cardButton.getBoundingClientRect().height,cardPaddingTop:Number.parseFloat(cardButtonStyle.paddingTop)||0,productGridGap:Number.parseFloat(gridStyle.rowGap)||Number.parseFloat(gridStyle.gap)||0,productGridColumns:gridStyle.gridTemplateColumns,sectionHeaderMarginBottom:Number.parseFloat(headerStyle.marginBottom)||0,sectionMarginTop:spacedSection?(Number.parseFloat(getComputedStyle(spacedSection).marginTop)||0):null,supportLineHeight:supportStyle?(Number.parseFloat(supportStyle.lineHeight)||0):null,categoryCardHeight:categoryCard?categoryCard.getBoundingClientRect().height:null,searchWidth:search.getBoundingClientRect().width,voiceWidth:voice.getBoundingClientRect().width,imageFit:image?getComputedStyle(image).objectFit:null,mediaWidth:mediaRect.width,mediaHeight:mediaRect.height,productId:card.getAttribute('data-product-id')||'',productReference:card.getAttribute('data-product-reference')||'',productRowTag:cardButton.tagName};});
 if(initial.scrollWidth>initial.clientWidth+1)throw new Error(`HORIZONTAL_OVERFLOW_${width}:${initial.scrollWidth}>${initial.clientWidth}`);if(initial.navButtons!==5)throw new Error(`BOTTOM_NAV_COUNT_${width}:${initial.navButtons}`);if(initial.productRowTag!=='A')throw new Error(`HOME_PRODUCT_ROW_LINK_SEMANTICS_${width}:${initial.productRowTag}`);if(initial.titleWhiteSpace==='nowrap'||initial.titleClamp!=='2')throw new Error(`PRODUCT_TITLE_TRUNCATION_CONTRACT_${width}:${initial.titleWhiteSpace}:${initial.titleClamp}`);if(initial.cardWidth>width||initial.searchWidth>width||initial.cardLeft<-1||initial.cardRight>width+1)throw new Error(`MOBILE_COMPONENT_WIDTH_OVERFLOW_${width}`);if(initial.voiceWidth<43)throw new Error(`VOICE_TOUCH_TARGET_TOO_SMALL_${width}:${initial.voiceWidth}`);if(initial.imageFit&&initial.imageFit!=='cover')throw new Error(`PRODUCT_IMAGE_OBJECT_FIT_${width}:${initial.imageFit}`);if(Math.abs(initial.mediaWidth-initial.mediaHeight)>1)throw new Error(`PRODUCT_MEDIA_NOT_SQUARE_${width}:${initial.mediaWidth}x${initial.mediaHeight}`);const expectedMedia=width<=350?74:82;if(Math.abs(initial.mediaWidth-expectedMedia)>1)throw new Error(`PRODUCT_MEDIA_SIZE_${width}:${initial.mediaWidth}!=${expectedMedia}`);if(!initial.productId||!initial.productReference)throw new Error(`PRODUCT_ROW_IDENTITY_MISSING_${width}`);if(initial.productGridColumns.trim().split(/\s+/).filter(Boolean).length!==1)throw new Error(`HOME_PRODUCT_GRID_COLUMN_REGRESSION_${width}:${initial.productGridColumns}`);if(initial.sectionMarginTop!==null&&initial.sectionMarginTop<39)throw new Error(`HOME_SECTION_RHYTHM_TOO_DENSE_${width}:${initial.sectionMarginTop}`);if(initial.sectionHeaderMarginBottom<19)throw new Error(`HOME_SECTION_HEADER_GAP_TOO_DENSE_${width}:${initial.sectionHeaderMarginBottom}`);if(initial.productGridGap>1)throw new Error(`HOME_PRODUCT_LIST_GAP_REGRESSION_${width}:${initial.productGridGap}`);if(initial.cardButtonHeight<(width<=350?99:107)||initial.cardButtonHeight>160)throw new Error(`HOME_PRODUCT_ROW_HEIGHT_${width}:${initial.cardButtonHeight}`);if(initial.cardPaddingTop<9||initial.cardPaddingTop>13)throw new Error(`HOME_PRODUCT_ROW_PADDING_${width}:${initial.cardPaddingTop}`);if(initial.categoryCardHeight!==null&&initial.categoryCardHeight<171)throw new Error(`HOME_CATEGORY_CARD_HEIGHT_TOO_DENSE_${width}:${initial.categoryCardHeight}`);if(initial.supportLineHeight!==null&&initial.supportLineHeight<22)throw new Error(`HOME_SUPPORT_COPY_LINE_HEIGHT_TOO_TIGHT_${width}:${initial.supportLineHeight}`);
 const compact=await verifyAdaptiveHeader(page,width);
 const deferredCount=await loadAllDeferredSections(page,width);await page.evaluate(()=>window.scrollTo({top:document.documentElement.scrollHeight,behavior:'auto'}));await page.waitForTimeout(120);const bottom=await page.evaluate(()=>{const nav=document.querySelector('nav[aria-label="Ana gezinme"]'),last=document.querySelector('.go-discover-all');if(!nav||!last)throw new Error('BOTTOM_NAV_OCCLUSION_TARGET_MISSING');const n=nav.getBoundingClientRect(),l=last.getBoundingClientRect(),root=document.documentElement;return{navTop:n.top,lastBottom:l.bottom,gap:n.top-l.bottom,docScrollWidth:root.scrollWidth,clientWidth:root.clientWidth,scrollY:window.scrollY,maxScroll:Math.max(0,root.scrollHeight-window.innerHeight)};});if(Math.abs(bottom.scrollY-bottom.maxScroll)>2)throw new Error(`BOTTOM_SCROLL_NOT_SETTLED_${width}:${bottom.scrollY}/${bottom.maxScroll}`);if(bottom.docScrollWidth>bottom.clientWidth+1)throw new Error(`BOTTOM_HORIZONTAL_OVERFLOW_${width}`);if(bottom.lastBottom>bottom.navTop-4)throw new Error(`BOTTOM_NAV_OCCLUSION_${width}:last=${bottom.lastBottom}:nav=${bottom.navTop}`);
 await search.focus();const focusOverflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);if(focusOverflow>1)throw new Error(`FOCUSED_SEARCH_OVERFLOW_${width}:${focusOverflow}`);
 const detailDock=await verifyProductDetailDock(page,width);const category=await verifyCategoryViewport(page,width);
 if(errors.length)throw new Error(`PAGE_ERRORS_${width}:${errors.join(' | ').slice(0,2000)}`);results.push({width,...initial,compact,deferredCount,bottom,detailDock,category});await page.screenshot({path:path.join(out,`premium-mobile-${width}.png`),fullPage:true});await context.close();console.log(`Premium Mobile viewport ${width}px passed.`);}}
finally{await browser.close();fs.writeFileSync(path.join(out,'premium-mobile-viewport-report.json'),JSON.stringify({baseUrl,widths,results,finishedAt:new Date().toISOString()},null,2));}
console.log('Golden Oremar Premium Mobile viewport E2E passed for Home, Product Detail and Category at 320/360/375/390/412/430 px with link product rows, Sesli mikrofon naming, clean quantity controls and fixed purchase-dock measurements.');
